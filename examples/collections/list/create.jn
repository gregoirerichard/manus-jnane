// Fonction pour créer une nouvelle liste dans le namespace collections.list

@name collections.list:create
@arg elements : List<Any> @doc("Éléments initiaux de la liste")
@field result : List<Any>
@view elements : List<Any>

{
    // Création d'une nouvelle liste avec les éléments fournis
    result = elements;
}
