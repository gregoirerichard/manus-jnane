// Fonction d'addition dans le namespace math
@name math:add
@name first : int @doc("First argument")
@name second : int @doc("Second argument")
@view result : int @doc("Result of the addition")

{
    // Implémentation simple de l'addition
    result = first + second;
}
