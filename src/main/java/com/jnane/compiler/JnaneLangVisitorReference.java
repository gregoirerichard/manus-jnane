package com.jnane.compiler;

import org.antlr.v4.runtime.tree.ParseTreeVisitor;

/**
 * Interface du visiteur pour l'AST du langage Jnane.
 * Cette interface étend le visiteur généré par ANTLR.
 * 
 * @deprecated Utilisez l'interface générée par ANTLR à la place.
 * Cette interface est conservée uniquement pour référence.
 */
@Deprecated
public interface JnaneLangVisitorReference<T> extends ParseTreeVisitor<T> {
    
    // Toutes les méthodes sont définies dans l'interface générée par ANTLR
    // Cette interface sert uniquement de point d'extension pour des méthodes personnalisées
    
}
